# Copy to frontend/.env.local for local dev. Set in Vercel Project → Settings → Environment Variables for deploys.

# Must be the browser-reachable API base (include /api if your Django API lives there).
# Vercel: set for Production *and* Preview, then redeploy — values are baked in at build time.
NEXT_PUBLIC_API_URL=http://localhost:8000/api
